# 06 — Build `Table` + tests

Type: task
Status: closed
Blocked by: 01, 02, 03, 04, 05

## Question

Build the destination: the `Table` component in `components/Table.tsx` implementing the rendering contract (02), filtering (03), sorting (04), server lifecycle (05), and the a11y roles from 01 — with the `TableColumn<T>`, `TableColumnType`, `TableConfig<T>`, `TableDataRequest`, `TableDataResponse<T>` types exported, dual-mode via `serverSide`, and `components/Table.test.tsx` (vitest + React Testing Library + jest-dom) covering both modes, matching `AppLayout.test.tsx` conventions.

This is the execution ticket; the build itself is the deliverable. Updates `CONTEXT.md` glossary entries as terms settle.