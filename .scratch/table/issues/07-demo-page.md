# 07 — Demo page wiring

Type: task
Status: closed
Blocked by: 06

## Question

Add the demo page in `app/` rendering `Table` with a small mock dataset — exercising both modes (server and local) and a representative mix of column types. Exact dataset/columns graduate from the rendering (02) and filtering (03) decisions; the page proves the component works in the running app.